import React from 'react';
import styled from 'styled-components';

//TODO finish implementing Content (currently in Card)
export const Content = (): JSX.Element => {
    return(
        <p></p>
    );
}